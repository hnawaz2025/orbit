{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd .. && npm run build --workspace @orbit/shared && cd app && npx expo export --platform web --clear",
  "outputDirectory": "dist",
  "installCommand": "cd .. && npm ci",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
